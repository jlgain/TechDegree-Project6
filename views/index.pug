extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Greetings, Potential Employers!

      p.lead.text-light
        | Provided below is a portfolio of projects I've developed for the Full Stack Tech Degree from TeamTreehouse.
        | The projects illustrate the skills and knowledge gained while pursuing a career as a software developer. 
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}` width='550' height='550')
            h5= project.project_name
      
